<%= form_with(model: tipo_participante, local: true) do |form| %>
  <% if tipo_participante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_participante.errors.count, "error") %> prohibited this tipo_participante from being saved:</h2>

      <ul>
      <% tipo_participante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
