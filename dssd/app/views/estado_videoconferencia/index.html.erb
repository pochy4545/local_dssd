<p id="notice"><%= notice %></p>

<h1>Estado Videoconferencia</h1>

<table>
  <thead>
    <tr>
      <th>Estado</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @estado_videoconferencia.each do |estado_videoconferencium| %>
      <tr>
        <td><%= estado_videoconferencium.estado %></td>
        <td><%= link_to 'Show', estado_videoconferencium %></td>
        <td><%= link_to 'Edit', edit_estado_videoconferencium_path(estado_videoconferencium) %></td>
        <td><%= link_to 'Destroy', estado_videoconferencium, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Estado Videoconferencium', new_estado_videoconferencium_path %>
