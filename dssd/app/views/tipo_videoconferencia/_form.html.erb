<%= form_with(model: tipo_videoconferencium, local: true) do |form| %>
  <% if tipo_videoconferencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_videoconferencium.errors.count, "error") %> prohibited this tipo_videoconferencium from being saved:</h2>

      <ul>
      <% tipo_videoconferencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
