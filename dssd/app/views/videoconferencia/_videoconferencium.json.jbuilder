json.extract! videoconferencium, :id, :fecha, :hora, :unidad, :estado, :tipo, :nro_causa, :motivo, :solicitante, :created_at, :updated_at
json.url videoconferencium_url(videoconferencium, format: :json)
